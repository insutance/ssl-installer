[tool.poetry]
name = "ssl-installer"
version = "0.1.0"
description = "SSL Certificate Installer"
authors = ["Your Name <your.email@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pyinstaller = "^6.11.0"


[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
